<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuena.supplier.mapper.CategoryMapper">
    <select id="selectById" parameterType="long" resultType="com.xuena.supplier.entity.CategoryDO">
        SELECT * FROM sp_category WHERE id = #{id} AND is_deleted = 0
    </select>
    
    <select id="selectByName" parameterType="string" resultType="com.xuena.supplier.entity.CategoryDO">
        SELECT * FROM sp_category WHERE name = #{name} AND is_deleted = 0
    </select>
    
    <select id="selectList" resultType="com.xuena.supplier.entity.CategoryDO">
        SELECT * FROM sp_category WHERE is_deleted = 0 ORDER BY gmt_create DESC
    </select>
    
    <insert id="insert" parameterType="com.xuena.supplier.entity.CategoryDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sp_category (name, description) VALUES (#{name}, #{description})
    </insert>
    
    <update id="update" parameterType="com.xuena.supplier.entity.CategoryDO">
        UPDATE sp_category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <update id="delete" parameterType="long">
        UPDATE sp_category SET is_deleted = 1 WHERE id = #{id}
    </update>
    
    <select id="countByName" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM sp_category WHERE name = #{name} AND is_deleted = 0
    </select>
    
    <select id="countByNameAndNotId" parameterType="map" resultType="int">
        SELECT COUNT(*) FROM sp_category WHERE name = #{name} AND id != #{id} AND is_deleted = 0
    </select>
</mapper>